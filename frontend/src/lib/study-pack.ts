/**
 * What the demo study pack holds. The hero card, the session strip under it
 * and the workflow story all quote these, so they can never disagree.
 */
export const STUDY_PACK = { flashcards: 24, quizzes: 3, strategies: 8 };
